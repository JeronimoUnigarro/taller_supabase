name: diary
description: A simple diary app using Clean Architecture and Supabase
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # Packages
  supabase_flutter: ^1.0.0
  flutter_dotenv: ^6.0.0
  get: ^4.6.5
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
